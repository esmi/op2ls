Run seq:
1. execute "mktap.sh" to create TAP device, and get the "TAP device name($TAP_IF)".
2. modify ipcop-run.sh: 
    TAP_IF="your TAP device name"
3. setup ICS and disable unused protocal.
4. execute "ipcop-run.sh" to run ipcop binary image.

PS:
    These script is written under cygwin environment.
    in win6 environment, please run these scripts under "as administrator" menu.
